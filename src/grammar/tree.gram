#JSGF V1.0;

grammar tree;

public <tree> = <address> <request> <requestTypes>;

public <vocabulary> = (<address> hello | hello <address>) * ;

public <extra> = ( yes | no | how | why | who | you);

<address> = (hey | please);

<request> = (tell | get the | what is);

<requestTypes> = (a joke | weather | time is it | day is it);